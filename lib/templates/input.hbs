/**
 * {{{description}}}
 */
export interface {{name}} {
    {{#each fields}}

    /**
     * {{{description}}}
     */
    {{name}}{{#if isNullable}}?{{/if}}: {{type}}{{#if isArray}}[]{{/if}};
    {{/each}}
}
